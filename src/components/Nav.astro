---
export interface Props {
  links?: Array<{ href: string; text: string }>;
}

const { links = [
  { href: "/", text: "Inicio" },
  { href: "#proyecto", text: "Proyecto" }
] } = Astro.props;
---

<nav class="main-nav">
  {links.map(link => <a href={link.href}>{link.text}</a>)}
</nav>